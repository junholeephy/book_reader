# ============================================================
# 저작물 — 절대 커밋하지 않는다
# 본문 텍스트와 목차는 각자의 PDF 에서 setup.sh 가 생성한다
# ============================================================
refs/*.txt
*.pdf

# ============================================================
# 개인 설정 · 런타임 데이터
# ============================================================
# PDF 경로 등 각자의 환경
config.json
# 질문·답변·학습 노트·잘라낸 이미지
qa/
# 렌더된 페이지 이미지
cache/

# ============================================================
# 제3자 워크플로 규칙 (AWS AI-DLC)
# book_reader 의 일부가 아니라 개발에 쓰는 도구이고,
# 라이선스 표기가 없어 이 저장소가 재배포할 근거가 없다.
# ============================================================
.aidlc-rule-details/

# ============================================================
# 내려받는 것 (setup.sh 가 받는다)
# ============================================================
reader/vendor/

# ============================================================
# 잡동사니
# ============================================================
__pycache__/
*.pyc
.DS_Store
.omc/
